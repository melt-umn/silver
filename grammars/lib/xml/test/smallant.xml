<project name='lib:xml' default='dist' basedir='.'>
  <description>Build the grammar lib:xml </description>

  <property environment='env'/>
  <property name='jg' location='/home/tedinski/cs/svn/silver/trunk/generated/'/>

  <target name='init'>

    <!-- Create the time stamp -->
    <tstamp>
      <format property='TIME' pattern='MM/dd/yyyy hh:mm aa'/>
    </tstamp>

    <mkdir dir='${bin}'/>
  </target>

</project>
