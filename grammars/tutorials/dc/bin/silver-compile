#!/bin/bash

# This script will use Silver to build an AG evaluator for the
# tutorials grammar and run ant to compile the resulting Java code.

time java -jar ../../../../jars/RunSilver.jar tutorials:dc:bin

# then run "ant"
ant

# then run "java -jar tutorials.dc.bin.jar 1 + 2"




#Ted, what is this command?  Is it obsolete?
#time java -Xss8M -Xmx3000M -jar ../jars/RunSilver.jar -I ../grammars $@ silver:composed:Default
