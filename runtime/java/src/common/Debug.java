package common;

public class Debug {
    
    public static DecoratedNode runDebug(DecoratedNode tree) {
        // Do interactive debug stuff

        // Return the original tree when the user exits
        return tree;
    }
}
